# s0f Chrome Extension Scaffold

```bash
cd ui/chrome
npm install
npm run dev
```

This MV3 extension currently logs action clicks. Future work should build a Native Messaging bridge connection to the Go daemon via `bmd-bridge`.
